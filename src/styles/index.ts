import { createStitches } from "@stitches/react";

export const { theme, styled } = createStitches({
  theme: {
    colors: {
      bodyBg: "#fbf7ff",
      white: "#fff",
      purpleBg: "#290742",
      darkBg: "#170027",
      buttonBg: "#9e6dc2",

      green: "#4fff4b"
    }
  }
});
